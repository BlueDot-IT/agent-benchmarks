Implement `normalizeTags` in `tags.mjs` so every test passes.

Preserve the exported API. Keep the implementation dependency-free and limit
changes to `tags.mjs`. Run the test suite before finishing.
