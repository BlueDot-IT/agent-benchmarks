The current workspace contains a small JavaScript module and its tests.

Fix the implementation so every test passes. Preserve the exported API, do not
weaken or delete tests, and keep the change limited to the implementation.
Run the test suite before finishing.
